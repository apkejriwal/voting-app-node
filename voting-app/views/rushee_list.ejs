<!-- views/rushee_list.ejs -->
<!doctype html>
<html>
<head>
    <title>Node Authentication</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css"> <!-- load bootstrap css -->
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css"> <!-- load fontawesome -->
    <style>
        body        { padding-top:80px; }
    </style>

    <script src="/socket.io/socket.io.js"></script>

    <script>

    // var socket = io();

    // function vote() {
    //     console.log("hit vote");
    //     socket.emit('login','test');
    // };

    // socket.on('redirect', function(destination) {
    //     console.log("hit redirect");
    //     window.location.href = destination;
    // });

    </script>

</head>
<body>


<div class="container">

<div class="col-sm-6 col-sm-offset-3">

    <h1><span class="fa fa-sign-in"></span>View All Rushees</h1>

          <table class="table">
                <thead>
                  <tr>
                    <th>First Name</th>
                    <th>Last Name </th>
                    <th>Major</th>
                    <th>Yes Votes</th>
                    <th>No Votes</th>
                    <th></th>
                    <th></th>
                  </tr>
                </thead>
                <tbody>

                <% for(var i=0; i < first_names_list.length; i++) {%>
                  <tr>
                    <td><%= first_names_list[i] %></td>
                    <td><%= last_names_list[i] %></td>
                    <td><%= majors_list[i] %></td>
                    <td><%= yes_votes_list[i] %></td>
                    <td><%= no_votes_list[i] %></td>
                    <td> 
                        <form action="/vote" method="post" type="hidden">
                            <div>           
                                <input type="hidden" value="<%= emails_list[i] %>" name="rushee_email">
                            </div>

                            <div>
                                <input type="hidden" value="Yes"  name="vote_value">
                            </div>
                        <button type="submit" class="btn btn-default btn-sm">Yes</button>
                        </form>
                    </td>

                    <td> 
                        <form action="/vote" method="post" type="hidden">
                            <div>
                                <input type="hidden" value ="<%= user.local.email %>" name="brother_email">
                            </div>
                            <div>           
                                <input type="hidden" value="<%= emails_list[i] %>" name="rushee_email">
                            </div>

                            <div>
                                <input type="hidden" value="No"  name="vote_value">
                            </div>
                        <button type="submit" class="btn btn-default btn-sm"">No</button>
                        </form>
                    </td>
                  </tr>
                <% } %>

                </tbody>
            </table>

    <a href="/profile" class="btn btn-default btn-sm">Back to Profile</a>

</div>

</div>
</body>
</html>

